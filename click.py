import pygame

class Click:

    def __init__(self):
        # Button to put in the file without instrument
        ## Il faut voir comment faire pour ne pas que ce soit si looooong
        ## Peut être utilisé une liste qui contient tout ? (voir le jeu que j'ai test)
        ...

    def clickButton(self, screen):
        screen.blit(self.do4, (0, 0))